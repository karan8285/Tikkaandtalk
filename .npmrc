# Force legacy peer deps for Vercel builds — @capacitor-firebase/messaging needs
# firebase@12 but web build uses firebase@11. The native Android build handles its
# own dependency resolution independently, so this only affects the web bundle.
legacy-peer-deps=true